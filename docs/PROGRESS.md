# Progress — ATLAS Control

> This file is the resume anchor. Builder agents update it after every completed task and at phase end.
> Resume command: **"Read CLAUDE.md, docs/PROGRESS.md, and docs/MASTER_PLAN.md. Continue with the next phase. Run acceptance criteria when done."**

## Current
Phase 6 — COMPLETE (deployed + migration proof 2026-07-06). Next: Phase 7.

## Done
- [x] Planning: design spec + master plan + phases 0–8 + execution playbook written and approved (2026-07-06).
- [x] Task 0.1 — server audit: hermes Up (digest sha256:b6e41c...), 265G free, 30G RAM. Restart-safety issue CONFIRMED (venv `hermes` entry script count = 0 → do NOT restart hermes container).
- [x] Task 0.2 — image pinned as `hermes-pinned:v0.16.0`; `atlas_net` created; `hermes` + `tabs_caddy_1` connected; in-network reachability verified (hermes:9119/api/status → 200, hermes:8642/health → ok).
- [x] Task 0.3 — full Hermes API contract captured to `02_dev_plan/phase0_captures/` (hermes-runs-contract.md, hermes-admin-contract.md, hermes-contract.json). Derived runs API from source to avoid burning the 429-limited quota.
- [x] Task 0.4 — Caddy/domain: site configured for `atlas.brandpreneur.net`; Caddy reloaded; Let’s Encrypt certificate issued; HTTPS returns expected 502.
- [x] Task 0.5 — repo scaffold created at `C:\dev\atlas-control`; plan docs copied; sanitized Phase-0 fixtures copied to `backend/tests/fixtures/`; backend/frontend sanity checks pass; pushed to `https://github.com/brandpreneuragency/atlas`; server clone exists at `/home/admin/atlas-control`.
- [x] Task 1.1 — Settings + DB core: `Settings`, SQLite async engine/session, WAL/foreign-key pragmas, migration runner, and `001_core.sql` implemented. Checks: `uv run ruff check .`, `uv run mypy app`, `uv run pytest -q` all pass.
- [x] Task 1.2 — Auth/session/CSRF/rate-limit/system routes implemented: first-boot password hashing, login/logout, signed `atlas_session` cookie, API auth middleware, CSRF middleware, public `/api/health`, `/api/me`, and killswitch settings. Checks: `uv run ruff check .`, `uv run mypy app`, `uv run pytest -q` all pass.
- [x] Task 1.3 — `HermesClient.health()` and `capabilities()` implemented with bearer auth, 10s default timeout, `HermesUnavailable` translation, and `/api/health` real-mode degradation. Checks: `uv run ruff check .`, `uv run mypy app`, `uv run pytest -q` all pass.
- [x] Task 1.4 — `/hermes/{path}` authenticated reverse-proxy stopgap implemented for HTTP methods with hop-by-hop header stripping and websocket 501 limitation. Checks: `uv run ruff check .`, `uv run mypy app`, `uv run pytest -q` all pass.
- [x] Task 1.5 — React SPA shell, login form, typed API client, Mission Control health card, placeholders, and Vite dev proxy implemented. Checks: `pnpm exec tsc --noEmit`, `pnpm exec eslint src`, `pnpm exec vitest run`, `pnpm build` all pass.
- [x] Task 2.1 — Event bus: `app/events.py` (`Broadcaster` set of `asyncio.Queue(maxsize=500)` with slow-consumer drop, `append_event(kind, source, summary, **payload)` → INSERT + publish) and `app/routers/events.py` (`GET /api/events?limit&before_id&kind` paginated newest-first; `GET /api/events/stream` SSE via `sse-starlette` emitting `event: atlas` + `data: {json}`, 20s heartbeat). The route subscribes eagerly so events appended between headers and first client pull are not lost. Tests: `tests/test_events.py` (3). Checks: ruff, mypy, pytest green (commit fde892b).
- [x] Task 2.2 — Full `HermesClient` per §7 (create_run/run_status/run_events/stop_run/approve_run/sessions/session_messages/create_session/chat_stream) + full `MockHermes` per §10. SSE parser splits on `\n\n`, parses `data:` JSON, ignores comments/heartbeats, tolerates unknown event types, ends on terminal `run.completed`/`run.failed`/`run.cancelled`. `test_mock_matches_interface` asserts method-name + `inspect.signature` parity. Tests: `tests/test_hermes_runs.py` (13). Checks green (commit c6e67da).
- [x] Task 2.3 — `HermesAdmin` (:9119) with `_token()` (regex `__HERMES_SESSION_TOKEN__\s*=\s*"([A-Za-z0-9_-]+)"` over index HTML, cached), `_authed_request` single retry-on-401 wrapper, full §7 surface (cron CRUD/pause/resume/trigger/delete, model info/options/set, env list/put/delete, analytics usage/models, logs, gateway_restart). `/api/env` masked values returned untouched. Tests: `tests/test_hermes_admin.py` (7) including `tests/fixtures/dashboard_index.html` sanitized snippet. Checks green (commit 299f0ac).
- [x] Task 2.4 — Agents endpoint + Hermes routers: `routers/agents.py` (`GET /api/agents` seeded on first boot with default "Hermes" agent from settings, live health/model/active_runs merged via MockHermes or HermesClient; adapter failure → `"status":"unreachable"` not 500) + `routers/hermes.py` expanded (`GET /api/hermes/sessions` proxies, `GET /api/hermes/sessions/{sid}/messages` proxies, `POST /api/hermes/chat` streams SSE tokens and creates `chat_threads` row on first message, `GET /api/hermes/threads` lists threads). New migration `002_chat_threads.sql` (split from §4 `002_workflows.sql` since Phase 2 needs it before Phase 5). Factory module `hermes/factory.py` shared by routers. Tests: `tests/test_routers_hermes.py` (5 including SSE chat via `stream_client`). Checks green (commit 59e3258).
- [x] Task 2.5 — Mission Control UI (complete, commit 2caacce): tests `frontend/src/__tests__/{feed.test.tsx,agentcard.test.tsx,chat.test.tsx}` added — feed newest-first render (icon + summary + relative time), SSE prepend without refetch (fetch spy), duplicate-id guard, AgentCard ok/unreachable dot + model + active runs, chat progressive token streaming via controlled ReadableStream + request payload assertion. Full suite: vitest 11/11, `tsc --noEmit`, `eslint src`, `pnpm build` all green. Prior WIP detail: `lib/sse.ts` EventSource wrapper (exponential backoff reconnect 1s→30s cap), `api/types.ts` expanded (AtlasEvent, AgentStatus, HermesSession, ChatMessage, EventKind), `api/fixtures.ts` (events, agent, sessions, messages fixtures), `components/feed/{EventRow,Feed}.tsx` (feed renders events newest-first, kind icon + summary + relative time), `components/cards/AgentCard.tsx` (status dot green/red, model, active runs), `pages/{Agent,Sessions,SessionDetail}.tsx` (Agent chat, Sessions table with search, SessionDetail transcript with chat bubbles + tool calls collapsed), `pages/MissionControl.tsx` updated (agent card row + feed column + stats), `routes.tsx` + `Nav.tsx` updated. `MockEventSource` test helper created. Checks: TypeScript compiles, vitest runs (existing 5 tests pass), `pnpm build` green (commit 57c7a06). **Still needed:** new frontend tests (`feed.test.tsx`, `agentcard.test.tsx`, `chat.test.tsx`), commit final.
- [x] Task 2.6 — Deploy + live verification (2026-07-06, commits c431ff8, 00020af, 6a9d195). Server `.env` found present (blocker cleared by user); SSH config entry added for `~/.ssh/atlas_control_deploy` so `git pull` works on the VPS. Deployed via `docker-compose build` + container recreate (compose v1 `KeyError: 'ContainerConfig'` workaround: build → `docker rm -f atlas_control` → `up -d`). **Live evidence:** `https://atlas.brandpreneur.net/api/health` → `{"status":"ok","db":"ok","hermes":{"runs_api":"ok"}}`; login 204 → `system.login` event row (id 1) in `/api/events`; SSE `/api/events/stream` delivered `event: atlas` + `system.login` JSON in real time while a second login fired (<2s); `/api/agents` → status `ok`, model `stepfun/step-3.7-flash:free` (NOT gpt-5.5 — the plan's expectation is stale), active_runs 0; `/api/hermes/sessions` → 200 sessions/page (live history; server caps a page at 200); cron transcript `cron_ae1df3bdd2c5_20260701_030006` rendered via `/messages`; chat POST `/api/hermes/chat` created Hermes session `api_1783315066_6f58db36` + `chat_threads` row id 1 + `hermes.run_event` feed event, streamed SSE — Hermes itself returned `error: No access token found for Nous Portal login` (see DECISION NEEDED). Backend 47 tests + ruff + mypy green; frontend tsc/eslint/vitest 11/build green.
- [x] Phase 2 — fully verified 2026-07-06 after user re-authed Hermes Nous Portal: live chat `POST /api/hermes/chat` "Reply PONG" streamed `event: token / data: PONG` then `done {"thread_id": 2}`.
- [x] Task 3.1 — path jail `app/files/safe_path.py` (commit 7eb4151): rejects null bytes, backslashes, absolute/`~` paths, drive letters, `..` (re-checked after one URL-decode pass), resolve+`is_relative_to` containment, ancestor symlink walk. All 12 adversarial tests pass (incl. symlink escape).
- [x] Task 3.2 — files API `routers/files.py` (commit 88cadf0): tree (dirs first, alpha, size+mtime), read (>2MB→413), write (`expected_mtime` stale→409 file unchanged; null creates), mkdir (exists→409), move/copy (`paths[]`+`dest`, refuse overwrite unless `overwrite:true`, bulk pre-validated atomically→400 on any bad path), delete (non-empty dir without `recursive:true`→400), multipart upload (201, jail-escape + bad filename→400). Every mutation → `file.*` event with summary (move: `"moved a → b"`). Mutations run in `anyio.to_thread`. Tests: `test_files_api.py` (16). Backend 75 tests + ruff + mypy green.
- [x] Task 3.3 — files UI (commit e8d632d): `pages/Files.tsx` + `components/files/{Tree,Preview,Editor,Toolbar,ConfirmDialog}.tsx`. Lazy per-dir tree fetch, checkbox multi-select, md preview via react-markdown with Edit toggle, CodeMirror editor (dirty dot, Ctrl+S, 409 conflict banner with Reload / Overwrite-anyway), Toolbar upload/move/delete with ConfirmDialog listing paths. Added dep `@testing-library/user-event`; api client gained `put` + `postForm`. Tests: `files.test.tsx` (5). Frontend 16 tests + tsc + eslint + build green.
- [x] Task 3.4 — deploy + live verification (2026-07-06): `GET /api/files/tree?path=03_brain` lists real vault dirs; traversal `read?path=../..%2f..%2fetc%2fpasswd` → **400** (no content); real note `ATLAS_CONTEXT.md` read OK; scratch write 01_inbox → 204; upload → 201; mkdir existing → 409; bulk move both files → `02_processed/01_short/` → 204; feed showed `file.created` ×2 + `file.changed` "moved … → …" ×2 within seconds; live stale-mtime write → 409 and file unchanged; cleanup delete → 204.
- [x] Task 4.1 — cron federation API (commit f112199): `GET/POST /api/hermes/cron`, `PUT /{id}`, `POST /{id}/pause|resume|trigger`, `DELETE /{id}` proxied via shared `get_hermes_admin` (app-state token cache); cron expr validated with `CronTrigger.from_crontab` → 400; every mutation appends `hermes.cron_changed` ("paused 'App Store Market Scout'" etc.); adapter failures append `hermes.error` + 502. Tests: `test_cron_routes.py` (10).
- [x] Task 4.2 — model/env/analytics/logs + model-prefs (commit 55ddf13⁻¹): `GET /api/hermes/model` merges info+options as `{current, options}`; `POST` proxies model/set + event; env list stays masked (NO reveal endpoint — deliberate); `PUT/DELETE /api/hermes/env`; analytics usage/models; logs?tail= as text/plain; `GET/PUT /api/settings/model-prefs` validates `{"favorites":[str],"hidden":[str]}` → 422 otherwise. Tests: `test_models_env_routes.py` (8).
- [x] Task 4.3 — kill switch v1 (commit 55ddf13): engage → pauses every ENABLED Hermes job, records ids in settings `killswitch_paused_jobs`, sets `global_pause=1`, `system.killswitch` event; release → resumes EXACTLY the recorded jobs (user-paused jobs stay paused), clears list; idempotent both ways. Tests: `test_killswitch.py` (3).
- [x] Task 4.4 — Automation + Models + Settings UI (commit ~e8d632d+): `lib/cron.ts` (`describeCron`: `*/30 * * * *`→"every 30 min", validation), `CronJobRow` (state badge with last_error tooltip, Pause/Resume/Run now/Edit/Delete), Automation page (Workflows empty state "coming in Phase 6", edit modal with live cron validation), Models page (current-model card, provider sections with Use-this-model, star/hide with `model_prefs` persistence, favorites float, "Show hidden (N)", search filter, provider-keys panel with add/delete+confirm), Settings v1 (password form, killswitch state, Phase 7/8 placeholders), `KillSwitch` top-bar button (red when engaged, confirm both ways). Tests: automation (4) + models (6). Frontend 26 tests + tsc + eslint + build green.
- [x] Task 4.5 — deploy + live verification (2026-07-06): real cron job listed with 429 last_error; **kill-switch jobs.json proof**: resume→`enabled: True state: scheduled`; engage→`{"paused":true}` and jobs.json `enabled: False state: paused`; release→restored `scheduled`; feed shows `system.killswitch` ENGAGED/released with counts. Model switched `stepfun/step-3.7-flash:free` → `deepseek/deepseek-v4-flash` → back (both 200, `current.model` confirmed each way). `FAKE_TEST_KEY` added (200) and deleted (200), never displayed unmasked. "Run now" triggered the scout job (200 + feed `triggered 'App Store Market Scout'`). Analytics usage returns 27 days of real data. Backend 98 tests green.

- [x] Task 5.1 — workflow CRUD + validation + versioning (commit bc8fd95): migration `003_workflows.sql` (§4 schema minus chat_threads, see deviation), `engine/schemas.py` (`Graph`/`Node`/`Edge` + `validate_graph` returning errors that name the offending node/edge id: unknown type, missing-node edge, 0/2 triggers, bad cron, jail-escaping paths), full CRUD + enable + versions + rollback-as-new-version. Tests: `test_workflow_crud.py` (11).
- [x] Task 5.2 — node executors + safe templating/expressions (commit a106795): `engine/context.py` — `render()` pure regex substitution (unknown keys stay literal + warning; `{{__import__('os')}}` stays a literal string) and `safe_expr()` hand-written AST-whitelist interpreter (Compare/BoolOp/UnaryOp/Name/Attribute/Subscript/Constant + comparison ops only; calls/lambdas/comprehensions/arithmetic → "disallowed expression"; lookups resolve only into the run context). `engine/nodes.py` — `NODE_EXECUTORS` registry for all §6 types: hermes.task (timeout/retries, events relayed as `hermes.run_event`), file.op (resolve_safe jail, write returns `{path, abs_path}`), logic.condition, notify.telegram/email (Phase-7 stubs, mocked in tests), shell.command (allowlist prefix, `create_subprocess_exec` + `shlex.split`, cwd jailed, timeout kills), gate.approval (approvals row + park marker). Tests: `test_templating.py` (12) + `test_nodes.py` (16).
- [x] Task 5.3 — engine core (commit dd50228): `engine/engine.py` per the mandated skeleton — submit guards (kill switch → `EnginePaused`, circuit breaker → refused run row `error="circuit breaker"` + event), queue-of-1 per workflow + global semaphore 2, BFS graph execution with per-step rows/events, condition branch edge selection + skipped marking, approval parking + `resume(decision)` (rejected follows `rejected` edge if present else finishes), budget accounting + `budget_exceeded`, restart recovery (`startup()`: running→failed "interrupted by restart", waiting_approval preserved), dry-run (MockHermes + `{data_dir}/dryrun` shadow jail, real factory never called), cancel (stops active hermes run). Tests: `test_engine.py` (12).
- [x] Task 5.4 — triggers + guards (commits 58e43fd, 243ae87): `engine/guards.py` (`Provenance` 60s TTL, `CircuitBreaker`, `Budget`), `engine/triggers.py` (`TriggerService.sync()` — one APScheduler job `wf-{id}` per enabled cron workflow, misfire grace 300s, tz from settings; file-drop routing with per-path debounce timers, glob filter, `.syncthing.*`/`*.tmp`/`.stfolder`/`.trash*` ignored, provenance suppression; `HookRateLimiter` 10/min/workflow), webhook route `POST /api/hooks/{wf}/{secret}` (404 wrong secret, 409 paused, 429 rate), manual `POST /api/workflows/{id}/run` (409 `{"detail":"paused"}` on kill switch). Engine + TriggerService wired into app lifespan (watcher only when atlas_root exists). Tests: `test_triggers.py` (14).
- [x] Task 5.5 — runs API + approvals (commit 8e5eda9): `GET /api/runs?workflow_id&status&limit`, `GET /api/runs/{id}` (run + ordered steps with parsed input/output/error), `POST /api/runs/{id}/cancel` (204; 409 if finished; stops active hermes run — mock-asserted), `GET /api/approvals?status=` + `POST /api/approvals/{id}/resolve` → `engine.resume`. `make check` gained `check-noexec` (greps `backend/app` for bare `eval|exec|compile|__import__` calls; `ast.parse(mode="eval")` exempted). Tests: `test_runs_api.py` (5). Backend suite: 168 green; ruff + mypy clean.
- [x] Task 5.5 Step 4 — live verification (2026-07-06, deployed commit 8e5eda9): smoke workflow created (wf 1) → **dry-run** run 1 `succeeded` using MockHermes (`mock-run-1`), file.op wrote to `/data/dryrun/...` shadow, NOTHING in ATLAS (`ls` confirmed absent); **real run** run 2 `succeeded` in 8.8s — hermes step output exactly `SMOKE_OK` (`run_ad0f8558...`, usage 18931 in / 89 out, cost_usd 0.01902), `smoke_test.md` written to real ATLAS containing `SMOKE_OK`; run detail showed both steps with input/output/usage; feed streamed the full sequence live (`run.started`, `step_started/finished` ×2, `hermes.run_event` relays incl. `message.delta`/`reasoning.available`, `run.finished`) for both runs. **Webhook from external curl (local machine, no session)**: correct secret → 202, wrong secret → 404. Cleanup: smoke_test.md deleted, workflows 1+2 deleted (204s), temp files removed.

**Phase 5 acceptance criteria (2026-07-06): ALL PASS** — (1) 168 backend tests green incl. all guard tests; (2) live smoke dry-run clean + real run end-to-end with history + live feed; (3) external webhook 202/404; (4) ruff+mypy+pytest+noexec+frontend(tsc/eslint/vitest/build) green; PROGRESS updated. `make` itself still absent on Windows — commands run directly (standing deviation).

- [x] Task 6.1 — canvas open/arrange/save (commit 9da9ed8): `flow/useGraph.ts` (`toFlow`/`fromFlow` lossless §6 round-trip — exact test; `nextNodeId` → `n<max+1>`; add/delete node w/ edge cascade; `save()` PUT + new version; dirty tracking), `flow/nodeTypes.tsx` (custom node per §6 type, category colors trigger=violet/hermes=blue/file=green/logic=amber/notify=cyan/shell=red/gate=orange, icon + label + one-line config summary, run-state badges), `flow/NodePalette.tsx` (drag source, `application/atlas-node`), `flow/Canvas.tsx` (React Flow with onDrop/screenToFlowPosition, edge labels = condition), `pages/WorkflowEditor.tsx` (+ routes `/automation/workflows/:id`, `/automation/runs/:id`), unsaved-changes guard (beforeunload + confirm on Back). Tests: canvas (6).
- [x] Task 6.2 — node config forms + validation (commit a3c7608): `flow/fields.tsx` (typed controlled fields, `validators` map mirroring backend validate_graph: cron via isValidCron, empty prompt, jail-escaping paths, op whitelist), `flow/ConfigPanel.tsx` (per-type forms: cron w/ live describeCron, path fields w/ Browse modal over `/api/files/tree`, prompt textarea + `{{trigger.*}}`/upstream-id template helper, condition input, shell command w/ allowlist display, gate message + notify checkboxes; `extractInvalidNodeIds` maps 422 detail → red node outline + banner in editor). Tests: configpanel (12).
- [x] Task 6.3 — run panel + live states + run detail (commit 33c2f82): backend step events now carry `node_id`/`status` payloads; `flow/runStates.ts` (`applyRunEvent` reducer: step_started→running, step_finished→succeeded/failed, waiting_approval→waiting), `flow/RunPanel.tsx` (Run/Dry-run POST with flag, recent-run list with status chip/duration/cost, links to RunDetail), editor subscribes to `/api/events/stream` filtered by workflow id → node badges light live + pulsing while running; `pages/RunDetail.tsx` (step list w/ collapsible input/output JSON, error text, inline Approve/Reject → `/api/approvals/{id}/resolve`). Tests: runpanel (5).
- [x] Task 6.4 — automation page + versions (commit ae1ecec): Workflows table (name→editor link, enabled toggle → POST enable, last-run status chip, cron schedule described, runs today, cost today), "New workflow" (creates lone `trigger.manual` graph → navigates to editor); `flow/VersionsDrawer.tsx` (versions w/ timestamps, confirm → POST rollback → editor reloads graph/version). Frontend suite: 53 tests + tsc + eslint + build green.
- [x] Fix during 6.5: mutating workflow routes (`enable/update/delete/rollback`) now call `TriggerService.sync()` (commit 28749ca) — previously cron jobs only registered at boot, so enabling a cron workflow at runtime never scheduled it.
- [x] Task 6.5 — migration proof + deploy (2026-07-06, evidence via curl per plan): deployed commit 28749ca; Hermes-native "App Store Market Scout" **paused** (kept, not deleted); **"App Store Scout v2"** created (wf 1) with `trigger.cron 0 8 * * *` → `hermes.task` (the live scout prompt pulled from the Hermes job, 490 chars) → `file.op write 04_reports/appstore/{{trigger.fired_at}}.md` → `notify.telegram` placeholder; enabled (runtime sync registered the daily job). **Dry-run** (run 1) succeeded — write went to `/data/dryrun/...`, ATLAS untouched. **Real run** (run 2, manual with `fired_at` payload) succeeded: hermes run `run_5f372ce4...`, 3,959,490/9,064 tokens (≈$3.97 at the $1/M approximation — the scout does heavy tool use; budget caps recommended), report `04_reports/appstore/2026-07-06T10-26-01.md` (2,299 bytes of real scout output), all 4 steps in run detail, feed streamed step_started/finished → run.finished live. Federated view shows BOTH: paused Hermes job + enabled v2 workflow (coexistence proven). Temp files cleaned.

**Phase 6 acceptance criteria (2026-07-06): ALL PASS** — (1) workflow buildable in UI (palette → connect → configure → save; versions + rollback tested); (2) live node states from SSE, failures red with error one click away, dry-run touches nothing; (3) Scout v2 exists, ran real once, output in `04_reports/appstore/`; (4) backend 168 tests + ruff + mypy + noexec, frontend 53 tests + tsc + eslint + build all green; PROGRESS updated.

## Phase-0 records (CAPTURED — later phases depend on these)
- **Run POST payload:** `POST /v1/runs {"input": "<prompt>"}` → 202 `{"run_id":"run_<hex>","status":"started"}`. (`input` required; may be message-list.)
- **Run terminal event + output field:** SSE field is **`event`** (not `type`); terminal = `run.completed` (also `run.failed`/`run.cancelled`); output text = `output` field; map to internal `output_text`.
- **Run usage fields:** `usage:{input_tokens,output_tokens,total_tokens}` on `run.completed` + on `GET /v1/runs/{id}` when completed.
- **9119 token:** injected as `window.__HERMES_SESSION_TOKEN__="..."`; regex `__HERMES_SESSION_TOKEN__\s*=\s*"([A-Za-z0-9_-]+)"`; header `Authorization: Bearer`; rotates per boot → cache + re-scrape on 401. `/api/status` public.
- **Host-header behavior:** `http://hermes:9119` accepted (200) over atlas_net — NO `Host:` override needed.
- **Subdomain chosen + DNS confirmed:** `atlas.brandpreneur.net` → `142.132.230.137`; Caddy issued a valid certificate; `https://atlas.brandpreneur.net/` returns expected `502` until the Phase-1 app container exists.
- **Caddyfile mount:** `/home/admin/tabs/Caddyfile` → `/etc/caddy/Caddyfile` (Task 0.4 Step 1 verified).

## Decisions / deviations
- 2026-07-06: **Task 2.5 reviews done by controller directly** — implementer/review subagent dispatches entered a delegation loop (agents re-delegating without producing work, 4 attempts, zero diffs). Controller wrote the three test files itself and performed spec+quality review inline against the task text. Deviation from EXECUTION_PLAYBOOK §1 ("controller never implements"), justified under the failure playbook (two consecutive non-productive dispatches).
- 2026-07-06: **vitest has no global auto-cleanup** (globals not enabled in vitest config) — new component tests call `cleanup()` + `vi.unstubAllGlobals()` in `afterEach`. Renders leak across tests otherwise.
- 2026-07-06: **SSE unit test transport** — httpx `ASGITransport.handle_async_request` awaits the entire ASGI app to completion and buffers the full body, so it CANNOT stream an infinite SSE response (the call hangs indefinitely, no middleware involved). To honor the Phase-2 spec ("use `client.stream('GET', ...)` and read one `data:` line with a 5s timeout") a `stream_client` conftest fixture spins a real uvicorn server on an ephemeral port and uses httpx's normal HTTP transport, which streams chunks as they arrive. Non-streaming tests keep the fast `ASGITransport`-based `app_client` fixture. Verified: a single `BaseHTTPMiddleware` + terminating SSE works through `ASGITransport`, but a stacked/infinite stream does not — so the limitation is httpx, not Starlette middleware.
- 2026-07-06: **chat_threads migration split** — MASTER_PLAN §4 bundles `chat_threads` in `002_workflows.sql` (Phase 5), but Phase 2's chat endpoint needs it now. Created `002_chat_threads.sql` with the `chat_threads` table. Phase 5 will add `003_workflows.sql` for workflows/runs/approvals (renumbered from §4's `002_workflows`). Deviation: migration numbering diverges from §4 but table schema is unchanged.
- 2026-07-06: Derived the runs API contract from `api_server.py` source instead of a live run — the model has an active 429 usage-limit and source is authoritative. No quota spent.
- 2026-07-06: `/api/cron/jobs` returns a **bare JSON array**, not `{"jobs":[]}`. Adapter parses a list.
- 2026-07-06: Only **ONE** Hermes cron job exists now (`App Store Market Scout`, paused, 429). The stale runtime doc's second "Digest Append" job is gone. Phase 6 migration proof targets the single job.
- 2026-07-06: `/api/env` is a **dict keyed by var name** with `{is_set,redacted_value,is_password,category,tools,...}`, not a flat list. Models provider-keys panel renders this shape.
- 2026-07-06: `relaxed_turing` orphan container removed after user approval (`docker rm -f relaxed_turing`).
- 2026-07-06: `make` is not installed in this Windows PowerShell environment; equivalent backend/frontend commands were run directly and passed. Use Git Bash or install make for the literal `make check` gate.
- 2026-07-06: Recovered missing host Caddyfile bind source: `/home/admin/tabs/Caddyfile` had become a directory; recreated it from live Caddy admin config plus the atlas site block, loaded the corrected config from `/tmp/Caddyfile.atlas.new`, and preserved `/home/admin/tabs/Caddyfile.dir-bak-phase0`.

- 2026-07-06: VPS deploy key installed; `/home/admin/atlas-control` cloned successfully from GitHub.
- 2026-07-06: **Live Hermes contract drift (Task 2.6)** — the running Hermes serves from `/opt/data/hermes-agent/`, NOT `/opt/hermes/` (which Phase 0 read; it is stale). Live differences fixed in `HermesClient`: (a) `GET /api/sessions` and `/messages` return an envelope `{"object":"list","data":[...]}`, not a bare list; (b) `POST /api/sessions` 400s without a JSON body (`{}` works) and nests the id as `session.id`; (c) chat stream emits **named SSE events** — tokens in `event: assistant.delta` `{"delta":...}`, terminal `event: done`, `event: error` `{"message":...}` (raised as `HermesUnavailable`); also emits `message.started`, `tool.progress` (incl. `_thinking`), `tool.started/completed/failed`, `assistant.completed`. Future phases must read `/opt/data/hermes-agent/gateway/platforms/api_server.py` for contract questions.
- 2026-07-06: **system.login was not reaching the SSE feed** — `auth.py` inserted the event row with raw SQL, bypassing the broadcaster. Fixed to call `append_event`; regression test `test_login_event_is_broadcast_live`.
- 2026-07-06: `ATLAS_PASSWORD` in the server `.env` contains a space — docker env_file takes the full line literally, so shell scripts must read it with `grep|cut`, never `source`.
- 2026-07-06: compose v1 on the VPS hits `KeyError: 'ContainerConfig'` when recreating containers built by BuildKit. Deploy recipe: `docker-compose build` → `docker rm -f atlas_control` → `docker-compose up -d`.

- 2026-07-06: **More live 9119 contract drift (Phase 4)** — `POST /api/model/set` requires `scope` field (`"main"` = primary slot; adapter now sends it); `DELETE /api/env/{key}` → 405, real form is `DELETE /api/env` with `{"key": ...}` body. Also `/api/model/options` providers is a **list of provider objects** (`{slug, name, is_current, models[]}`), not a dict — Models page should be checked against this live shape in a future pass. Custom keys (e.g. FAKE_TEST_KEY) do not appear in the GET /api/env catalog (it lists known vars only) though PUT/DELETE succeed.
- 2026-07-06: Phase-4 plan expected "two real cron jobs" and a "digest job" — stale; only `App Store Market Scout` exists (Phase-0 record). Live verification used it for pause/resume/trigger.

- 2026-07-06: **Phase-5 migration is `003_workflows.sql`** (002 was taken by chat_threads early-landing); content is §4's `002_workflows` verbatim minus `chat_threads`. Migration runner splits on `;` — SQL comments must not contain semicolons.
- 2026-07-06: **Run cost model**: Hermes usage events carry tokens but no cost, so the engine approximates `cost_usd = (input+output tokens) × $1/1M` (constant `COST_USD_PER_TOKEN` in engine.py). Budget tests depend on this; revisit when Hermes reports real cost.
- 2026-07-06: **Approvals router added in Task 5.5** (routers/approvals.py) — not in the 5.5 file list but required so `engine.resume` is reachable per the §5 route surface and the 5.3 skeleton comment ("resume() from approvals router").
- 2026-07-06: Webhook + manual-run routes live in `routers/workflows.py` (`hooks_router` without session auth); Task 5.4 lists no router file, this was the least-new-files placement.
- 2026-07-06: **Scout v2 real run cost ≈$3.97** (3,959,490 input / 9,064 output tokens at the $1/M approximation) — the live scout prompt does heavy tool use, far above the smoke-test scale. `App Store Scout v2` was left in PROGRESS without a `budget_usd_per_run` cap; recommend setting one (or `max_runs_per_hour`) before its daily 08:00 cron fires, since the engine's budget guard only stops a run already in progress once cost is added per step, not before it starts.

- 2026-07-06: User removed "App Store Scout v2" (workflow id 1) after the Phase 6 migration proof, citing its ~$3.97/run cost. `DELETE /api/workflows/1` → 204, cascade clean, `TriggerService.sync()` unregistered its cron job. The Hermes-native "App Store Market Scout" job remains **paused** — no app-store scouting automation is currently running (neither the Hermes job nor a workflow).

## DECISION NEEDED
- No automation currently covers app-store scouting (Scout v2 deleted; Hermes-native job still paused from the migration proof). Resume the Hermes-native job, rebuild a budget-capped workflow version, or leave both off? Awaiting user direction.
